class Calculadora:
    """"clase para las operaciones basicas""""
    def _init_(self):
        self.valor_1 = 0
        self.valor_2 = 0
        self.resultado = 0

        def sumar(self):
            """"operacion que suma los valores seteados en la calculadora""""
            self.resultado = self.valor_1 + self.valor_2

        def restar(self):
            """"operacion que resta los valores seteados en la calculadora""""
            self.resultado = self.valor_1 - self.valor_2

        def multiplicar(self):
            """"operacion que multiplica los valores seteados en la calculadora""""
            self.resultado = self.valor_1 * self.valor_2

        def dividir(self):
            """"operacion que divide los valores seteados en la calculadora""""
            self.resultado = self.valor_1 / self.valor_2